name: qa_automation_app
description: Internal QA Automation Tool
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^6.1.1
  http: ^1.2.0
  provider: ^6.1.1

flutter:
  uses-material-design: true
